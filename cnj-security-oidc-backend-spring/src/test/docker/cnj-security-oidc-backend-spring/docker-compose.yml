# docker-compose.yml
# ----------------------------------------------------------------------------
# Composes Docker containers and volumes to a runnable Spring Boot application
# ----------------------------------------------------------------------------
#
---
version: '3'

services:

  backend:
    image: docker.at41tools.k8s.aws.msgoat.eu/cloudtrain/cnj-security-oidc-backend-spring:${project.version}
    ports:
      - "38080:8080"
    environment:
      SPRING_PROFILES_ACTIVE: cloud
      CNAP_CLOUD: kubernetes
      SPRING_SECURITY_OAUTH2_RESOURCESERVER_JWT_JWK_SET_URI: https://iam.at41tools.k8s.aws.msgoat.eu/auth/realms/cloudtrain/protocol/openid-connect/certs
#      LOGGING_LEVEL_ORG_KEYCLOAK: DEBUG
#      LOGGING_LEVEL_ORG_SPRINGFRAMEWORK_SECURITY: DEBUG





  