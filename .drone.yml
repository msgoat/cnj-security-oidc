---
kind: pipeline
type: kubernetes
name: cnj-security-oidc-build

steps:
  - name: test-commit-stage
    image: docker.at41tools.k8s.aws.msgoat.eu/cloudtrain/cnj-drone-build:1.0.0
    pull: always
    commands:
      - mvn clean deploy -f cnj-security-oidc-backend-test/pom.xml --batch-mode --fail-fast --errors -V -U -Dsurefire.useFile=false --activate-profiles commit-stage
#  - name: javaee-commit-stage
#    image: docker.at41tools.k8s.aws.msgoat.eu/cloudtrain/cnj-drone-build:1.0.0
#    privileged: true
#    commands:
#      - start-docker.sh
#      - mvn clean deploy -f cnj-security-oidc-backend-javaee/pom.xml --batch-mode --fail-fast --errors -V -U -Dsurefire.useFile=false --activate-profiles commit-stage -Dtest.target.route=http://localhost:38080
#  - name: javaee-integration-test-stage
#    image: docker.at41tools.k8s.aws.msgoat.eu/cloudtrain/cnj-drone-build:1.0.0
#    commands:
#      - mvn clean verify -f cnj-security-oidc-backend-javaee/pom.xml --batch-mode --fail-fast --errors -V -U -Dsurefire.useFile=false --activate-profiles integration-test-stage
  - name: micro-commit-stage
    image: docker.at41tools.k8s.aws.msgoat.eu/cloudtrain/cnj-drone-build:1.0.0
    privileged: true
    commands:
      - start-docker.sh
      - mvn clean deploy -f cnj-security-oidc-backend-micro/pom.xml --batch-mode --fail-fast --errors -V -U -Dsurefire.useFile=false --activate-profiles commit-stage -Dtest.target.route=http://localhost:38080
  - name: micro-integration-test-stage
    image: docker.at41tools.k8s.aws.msgoat.eu/cloudtrain/cnj-drone-build:1.0.0
    privileged: true
    commands:
      - start-docker.sh
      - mvn clean verify -f cnj-security-oidc-backend-micro/pom.xml --batch-mode --fail-fast --errors -V -U -Dsurefire.useFile=false --activate-profiles integration-test-stage
  - name: spring-commit-stage
    image: docker.at41tools.k8s.aws.msgoat.eu/cloudtrain/cnj-drone-build:1.0.0
    privileged: true
    commands:
      - start-docker.sh
      - mvn clean deploy -f cnj-security-oidc-backend-spring/pom.xml --batch-mode --fail-fast --errors -V -U -Dsurefire.useFile=false --activate-profiles commit-stage -Dtest.target.route=http://localhost:38080
  - name: spring-integration-test-stage
    image: docker.at41tools.k8s.aws.msgoat.eu/cloudtrain/cnj-drone-build:1.0.0
    commands:
      - mvn clean verify -f cnj-security-oidc-backend-spring/pom.xml --batch-mode --fail-fast --errors -V -U -Dsurefire.useFile=false --activate-profiles integration-test-stage
